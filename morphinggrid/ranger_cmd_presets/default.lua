morphinggrid.ranger_cmd_presets.default = {
    toggle_helmet = {
        description = "Takes off or puts on a ranger's helmet. The helmet must me wielded to be put on.",
        func = function(name, text, ranger)
            
        end
    }
}
